import { Typography } from '@mui/material';
import React from 'react';

const AboutUs = () => {
    return (
        <div>
            <Typography variant='h4' sx={{ fontWeight: 'medium', m: 2, textAlign: 'center' }}>About Us</Typography>
        </div>
    );
};

export default AboutUs;